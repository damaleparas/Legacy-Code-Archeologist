# ── Runtime ──────────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
requests>=2.31.0
httpx>=0.27.0           # async HTTP used by tests / future agents

# ── RL / Training ────────────────────────────────────────────────────────────
gymnasium>=0.29.1       # optional: wrap env as a Gym env for SB3 / RLlib
stable-baselines3>=2.3.0  # optional baseline agent training

# ── OpenEnv (install from source until public release) ───────────────────────
# openenv-core>=0.1.0   # uncomment when published to PyPI
# git+https://github.com/facebookresearch/openenv.git#egg=openenv-core

# ── Dev / Test ───────────────────────────────────────────────────────────────
pytest>=8.1.0
pytest-asyncio>=0.23.0
pytest-timeout>=2.3.0
black>=24.4.0
ruff>=0.4.0
mypy>=1.10.0
